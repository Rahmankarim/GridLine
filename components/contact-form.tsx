"use client"
import { FormEvent, useState } from 'react'
import { ArrowUpRight, Check } from 'lucide-react'
export function ContactForm() { const [sent, setSent] = useState(false); const [error, setError] = useState(''); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = e.currentTarget; const data = new FormData(form); if (!data.get('name') || !data.get('email') || !data.get('message')) { setError('Please fill in all fields.'); return }; setError(''); setSent(true); form.reset() } if (sent) return <div className="success-message"><span><Check size={20} /></span><h2>Message received.</h2><p>Thanks for reaching out. A member of our team will be in touch shortly.</p></div>; return <form className="contact-form" onSubmit={submit} noValidate><label>Name<input name="name" type="text" placeholder="Your name" /></label><label>Email<input name="email" type="email" placeholder="you@company.com" /></label><label>How can we help?<textarea name="message" rows={5} placeholder="Tell us a little about your energy challenge..." /></label>{error && <p className="form-error">{error}</p>}<button className="button button-accent" type="submit">Send message <ArrowUpRight size={16} /></button></form> }

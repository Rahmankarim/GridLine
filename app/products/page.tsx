import type { Metadata } from 'next'
import Link from 'next/link'
import { ArrowUpRight, BarChart3, BellRing, BrainCircuit, Cable, CircleGauge, Settings2 } from 'lucide-react'
import { Reveal } from '@/components/reveal'
export const metadata: Metadata = { title: 'Platform | Gridline Energy', description: 'A connected intelligence layer for energy-intensive operations.', openGraph: { title: 'Gridline Platform', description: 'Monitor, understand, and improve energy performance across every site.', type: 'website' } }
const products = [{ icon: CircleGauge, number: '01', title: 'Gridline Monitor', text: 'The live operational picture for every meter, asset, and site.', features: ['Real-time asset visibility', 'Multi-site performance views', 'Custom energy baselines'] }, { icon: BrainCircuit, number: '02', title: 'Gridline Insights', text: 'The context behind the numbers, delivered before you have to ask.', features: ['Automated anomaly detection', 'Root-cause analysis', 'Opportunity prioritization'] }, { icon: Settings2, number: '03', title: 'Gridline Control', text: 'The confident next step from insight to intervention.', features: ['Workflow automation', 'Rules-based controls', 'Impact verification'] }]
export default function Products() { return <main><section className="page-hero"><div className="container page-hero-grid"><div><p className="eyebrow">The platform</p><h1>See more.<br /><em>Waste less.</em></h1></div><p className="page-hero-lede">A practical intelligence layer that makes energy data useful to the people responsible for making things run better.</p></div></section><section className="section products-section"><div className="container"><div className="product-list">{products.map((p, i) => <Reveal key={p.title} delay={i * .1}><article className="platform-card"><div className="platform-icon"><p>{p.number}</p><p.icon size={28} strokeWidth={1.4} /></div><div className="platform-copy"><p className="eyebrow">{p.title}</p><h2>{p.text}</h2><ul>{p.features.map(f => <li key={f}><span />{f}</li>)}</ul></div><Link href="/contact" className="circle-link" aria-label={`Learn about ${p.title}`}><ArrowUpRight size={19} /></Link></article></Reveal>)}</div></div></section><section className="metrics-band"><div className="container metrics-grid"><div><strong>24<span>/7</span></strong><p>visibility across your operation</p></div><div><strong>3.2<span>x</span></strong><p>faster issue resolution</p></div><div><strong>18<span>%</span></strong><p>average efficiency uplift</p></div></div></section></main> }
